# library-management-sql
This repository contains a beginner-friendly SQL database project that models a simple library management system. It demonstrates the creation of database tables, insertion of sample data, and the execution of common SQL operations such as querying, joining tables and updating records,
